# telemetry/ — observability (skeleton)

Uniform analytics, logging & telemetry across the sibling projects. Skeleton only — the full-build
plan is **`../history/rfcs/RFC-003-telemetry.md`** (OpenTelemetry schema, collector, per-project
wiring, dashboards).

Goal: one convention that works the same for every sibling repo, so runs are comparable and agentic
loops have real signal.
